{{ if eq .chezmoi.osRelease.id "arch" -}}
#!/bin/sh

sudo pacman -S zsh ripgrep fzf lsd yazi

{{ else if eq .chezmoi.osRelease.id "ubuntu" "debian" -}}
#!/bin/sh

sudo apt install zsh ripgrep fzf
{{ if not (and (eq .chezmoi.osRelease.id "debian") (le .chezmoi.osRelease.versionID 11)) -}}
sudo apt install lsd
{{ end -}}
{{ end -}}
